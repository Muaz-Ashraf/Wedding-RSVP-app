import { Card, CardContent, CardHeader } from "@mui/material";
import React from "react";

const WeddingCard = () => {
  return (
    <Card sx={{ borderRadius: "0.1rem", bgcolor: "lightpink" }}>
      <CardContent>Invitation</CardContent>
    </Card>
  );
};

export default WeddingCard;
